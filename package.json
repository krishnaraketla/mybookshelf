{
    "name": "portfolio",
    "version": "1.0.0",
    "scripts": {
    "build": "npm --prefix frontend install frontend && npm --prefix frontend run build",
    "start-backend": "npm --prefix backend install backend && npm --prefix backend start",
    "heroku-postbuild": "npm run build"
    }
}